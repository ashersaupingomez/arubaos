const createClient = require('./createClient');
const getAccessPoints = require('./getAccessPoints');
const login = require('./login');
const logout = require('./logout');
const renameAccessPoint = require('./renameAccessPoint');
const useClient = require('./useClient');

module.exports = {
  createClient,
  getAccessPoints,
  login,
  logout,
  renameAccessPoint,
  useClient,
};
